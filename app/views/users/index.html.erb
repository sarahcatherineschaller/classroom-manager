<h1>Welcome to <%= current_user.title %> <%= current_user.first_name %> <%= current_user.last_name %>'s Classroom Manager!</h1>

<h3><%= link_to 'My Classrooms', user_classrooms_path(current_user) %></h3>

<h3><%= link_to 'All Students', students_path %></h3>

<h3>My Current Students:</h3>
<% current_user.students.order(:last_name).each do |student| %>
	<%= link_to student.full_name, student %>
	<br>
<% end %>